# © 2024–2026 TiltCheck Ecosystem. All Rights Reserved. Last Updated: 2026-05-03
# DEPRECATED: apps/hub (Cloudflare Worker) was removed. hub.tiltcheck.me should
# resolve to the user-dashboard service through whatever public routing is
# actually active for production (custom domain, proxy, or optional tunnel).
# If a Cloudflare Tunnel is used, .github/workflows/configure-tunnel.yml manages it.
# This file is kept for history only and will not trigger.
name: Deploy Hub to Cloudflare Workers (DEPRECATED)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type CONFIRM to acknowledge this workflow is deprecated"
        required: true

permissions:
  contents: read

jobs:
  noop:
    name: Deprecated
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated. apps/hub no longer exists."
          echo "hub.tiltcheck.me should resolve to user-dashboard through the active public ingress path."
          echo "If you are using a tunnel, use .github/workflows/configure-tunnel.yml to manage tunnel routes."
          exit 1
