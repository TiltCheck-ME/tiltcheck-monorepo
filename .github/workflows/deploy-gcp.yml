# © 2024–2026 TiltCheck Ecosystem. All Rights Reserved. Last Updated: 2026-04-04
name: Deploy Services to GCP Cloud Run

on:
  workflow_dispatch:
    inputs:
      service:
        description: 'Service to deploy (leave empty to use path detection)'
        required: false
        type: choice
        options: ['', api, web, discord-bot, trust-rollup, control-room, game-arena, user-dashboard]
  push:
    branches:
      - main
    paths:
      - 'apps/api/**'
      - 'apps/web/**'
      - 'apps/discord-bot/**'
      - 'apps/trust-rollup/**'
      - 'apps/control-room/**'
      - 'apps/game-arena/**'
      - 'apps/user-dashboard/**'
      - 'packages/**'
      - 'modules/**'
      - 'cloudbuild.yaml'
      - 'Dockerfile'

env:
  PROJECT_ID: tiltchcek
  REGION: us-central1
  REPOSITORY: tiltcheck-services

permissions:
  contents: read

jobs:
  changes:
    name: Detect Changed Services
    runs-on: ubuntu-latest
    outputs:
      api: ${{ inputs.service == 'api' || (inputs.service == '' && steps.filter.outputs.api == 'true') }}
      web: ${{ inputs.service == 'web' || (inputs.service == '' && steps.filter.outputs.web == 'true') }}
      discord-bot: ${{ inputs.service == 'discord-bot' || (inputs.service == '' && steps.filter.outputs['discord-bot'] == 'true') }}
      trust-rollup: ${{ inputs.service == 'trust-rollup' || (inputs.service == '' && steps.filter.outputs['trust-rollup'] == 'true') }}
      control-room: ${{ inputs.service == 'control-room' || (inputs.service == '' && steps.filter.outputs['control-room'] == 'true') }}
      game-arena: ${{ inputs.service == 'game-arena' || (inputs.service == '' && steps.filter.outputs['game-arena'] == 'true') }}
      user-dashboard: ${{ inputs.service == 'user-dashboard' || (inputs.service == '' && steps.filter.outputs['user-dashboard'] == 'true') }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            api:
              - 'apps/api/**'
              - 'packages/**'
              - 'modules/**'
              - 'cloudbuild.yaml'
              - 'Dockerfile'
            web:
              - 'apps/web/**'
              - 'packages/**'
              - 'cloudbuild.yaml'
              - 'Dockerfile'
            discord-bot:
              - 'apps/discord-bot/**'
              - 'packages/**'
              - 'modules/**'
              - 'cloudbuild.yaml'
              - 'Dockerfile'
            trust-rollup:
              - 'apps/trust-rollup/**'
              - 'packages/**'
              - 'modules/**'
              - 'cloudbuild.yaml'
              - 'Dockerfile'
            control-room:
              - 'apps/control-room/**'
              - 'packages/**'
              - 'modules/**'
              - 'cloudbuild.yaml'
              - 'Dockerfile'
            game-arena:
              - 'apps/game-arena/**'
              - 'packages/**'
              - 'modules/**'
              - 'cloudbuild.yaml'
              - 'Dockerfile'
            user-dashboard:
              - 'apps/user-dashboard/**'
              - 'packages/**'
              - 'cloudbuild.yaml'
              - 'Dockerfile'

  deploy:
    name: Deploy ${{ matrix.service }}
    runs-on: ubuntu-latest
    needs: changes
    if: |
      needs.changes.outputs.api == 'true' ||
      needs.changes.outputs.web == 'true' ||
      needs.changes.outputs['discord-bot'] == 'true' ||
      needs.changes.outputs['trust-rollup'] == 'true' ||
      needs.changes.outputs['control-room'] == 'true' ||
      needs.changes.outputs['game-arena'] == 'true' ||
      needs.changes.outputs['user-dashboard'] == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - service: api
            changed: ${{ needs.changes.outputs.api }}
          - service: web
            changed: ${{ needs.changes.outputs.web }}
          - service: discord-bot
            changed: ${{ needs.changes.outputs['discord-bot'] }}
          - service: trust-rollup
            changed: ${{ needs.changes.outputs['trust-rollup'] }}
          - service: control-room
            changed: ${{ needs.changes.outputs['control-room'] }}
          - service: game-arena
            changed: ${{ needs.changes.outputs['game-arena'] }}
          - service: user-dashboard
            changed: ${{ needs.changes.outputs['user-dashboard'] }}

    steps:
      - name: Checkout
        if: matrix.changed == 'true'
        uses: actions/checkout@v4

      - name: Authenticate to GCP
        if: matrix.changed == 'true'
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        if: matrix.changed == 'true'
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ env.PROJECT_ID }}

      - name: Submit Cloud Build
        if: matrix.changed == 'true'
        run: |
          gcloud builds submit \
            --config=cloudbuild.yaml \
            --substitutions="_SERVICE_NAME=${{ matrix.service }},_REPOSITORY=${{ env.REPOSITORY }},_REGION=${{ env.REGION }}" \
            --project=${{ env.PROJECT_ID }} \
            .
