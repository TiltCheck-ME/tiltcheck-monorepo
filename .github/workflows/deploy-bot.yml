name: Deploy JustTheTip Bot

on:
  push:
    branches: [main]
    paths:
      - 'apps/justthetip-bot/**'
      - 'packages/**'
      - '.github/workflows/deploy-bot.yml'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: pnpm run build

      - name: Build Docker image
        run: |
          docker build -f apps/justthetip-bot/Dockerfile -t justthetip-bot:${{ github.sha }} .
          docker tag justthetip-bot:${{ github.sha }} justthetip-bot:latest

      - name: Log in to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push to Docker Hub
        if: github.event_name != 'pull_request'
        run: |
          docker tag justthetip-bot:latest ${{ secrets.DOCKER_USERNAME }}/justthetip-bot:latest
          docker tag justthetip-bot:latest ${{ secrets.DOCKER_USERNAME }}/justthetip-bot:${{ github.sha }}
          docker push ${{ secrets.DOCKER_USERNAME }}/justthetip-bot:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/justthetip-bot:${{ github.sha }}

      - name: Deploy to server
        if: github.event_name != 'pull_request'
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            cd /opt/tiltcheck
            docker compose pull justthetip-bot
            docker compose up -d justthetip-bot
            docker system prune -f
