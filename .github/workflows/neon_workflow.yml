# © 2024–2026 TiltCheck Ecosystem. All Rights Reserved. Last Updated: 2026-05-07

name: Create/Delete Branch for Pull Request

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - closed

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  setup:
    name: Setup
    outputs:
      branch: ${{ steps.branch_name.outputs.current_branch }}
    runs-on: ubuntu-latest
    steps:
      - name: Get branch name
        id: branch_name
        uses: tj-actions/branch-names@v8

  create_neon_branch:
    name: Create Neon Branch
    outputs:
      db_url: ${{ steps.create_neon_branch_encode.outputs.db_url }}
      db_url_with_pooler: ${{ steps.create_neon_branch_encode.outputs.db_url_with_pooler }}
    needs: setup
    if: |
      github.event_name == 'pull_request' && (
      github.event.action == 'synchronize'
      || github.event.action == 'opened'
      || github.event.action == 'reopened')
    runs-on: ubuntu-latest
    env:
      NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
    steps:
      - name: Skip Neon Branch Without API Key
        if: env.NEON_API_KEY == ''
        run: echo "::notice::Skipping Neon branch creation because NEON_API_KEY is unavailable in this PR context."

      - name: Get branch expiration date as an env variable (2 weeks from now)
        if: env.NEON_API_KEY != ''
        id: get_expiration_date
        run: echo "EXPIRES_AT=$(date -u --date '+14 days' +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_ENV"

      - name: Create Neon Branch
        if: env.NEON_API_KEY != ''
        continue-on-error: true
        id: create_neon_branch
        uses: neondatabase/create-branch-action@v6
        with:
          project_id: ${{ vars.NEON_PROJECT_ID }}
          branch_name: preview/pr-${{ github.event.number }}-${{ needs.setup.outputs.branch }}
          api_key: ${{ env.NEON_API_KEY }}
          expires_at: ${{ env.EXPIRES_AT }}

      - name: Report Optional Neon Branch Failure
        if: env.NEON_API_KEY != '' && steps.create_neon_branch.outcome == 'failure'
        run: echo "::warning::Neon preview branch creation failed; continuing because preview database provisioning is optional for CI validation."

  # The step above creates a new Neon branch.
  # You may want to do something with the new branch, such as run migrations, run tests
  # on it, or send the connection details to a hosting platform environment.
  # The branch DATABASE_URL is available to you via:
  # "${{ steps.create_neon_branch.outputs.db_url_with_pooler }}".
  # It's important you don't log the DATABASE_URL as output as it contains a username and
  # password for your database.
  # For example, you can uncomment the lines below to run a database migration command:
  #      - name: Run Migrations
  #        run: npm run db:migrate
  #        env:
  #          # to use pooled connection
  #          DATABASE_URL: "${{ steps.create_neon_branch.outputs.db_url_with_pooler }}"
  #          # OR to use unpooled connection
  #          # DATABASE_URL: "${{ steps.create_neon_branch.outputs.db_url }}"

  # Following the step above, which runs database migrations, you may want to check
  # for schema changes in your database. We recommend using the following action to
  # post a comment to your pull request with the schema diff. For this action to work,
  # you also need to give permissions to the workflow job to be able to post comments
  # and read your repository contents. Add the following permissions to the workflow job:
  #
  # permissions:
  #   contents: read
  #   pull-requests: write
  #
  # You can also check out https://github.com/neondatabase/schema-diff-action for more
  # information on how to use the schema diff action.
  # You can uncomment the lines below to enable the schema diff action.
  #      - name: Post Schema Diff Comment to PR
  #        uses: neondatabase/schema-diff-action@v1
  #        with:
  #          project_id: ${{ vars.NEON_PROJECT_ID }}
  #          compare_branch: preview/pr-${{ github.event.number }}-${{ needs.setup.outputs.branch }}
  #          api_key: ${{ secrets.NEON_API_KEY }}

  delete_neon_branch:
    name: Delete Neon Branch
    needs: setup
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    env:
      NEON_API_KEY: ${{ secrets.NEON_API_KEY }}
    steps:
      - name: Skip Neon Branch Cleanup Without API Key
        if: env.NEON_API_KEY == ''
        run: echo "::notice::Skipping Neon branch cleanup because NEON_API_KEY is unavailable in this PR context."

      - name: Install Neon CLI
        if: env.NEON_API_KEY != ''
        run: npm i -g neonctl@v2

      - name: Delete Neon Branch
        if: env.NEON_API_KEY != ''
        env:
          NEON_API_HOST: https://console.neon.tech/api/v2
          NEON_BRANCH_NAME: preview/pr-${{ github.event.number }}-${{ needs.setup.outputs.branch }}
          NEON_PROJECT_ID: ${{ vars.NEON_PROJECT_ID }}
        run: |
          set +e
          output=$(neonctl branches delete "$NEON_BRANCH_NAME" --project-id "$NEON_PROJECT_ID" 2>&1)
          status=$?
          set -e

          printf '%s\n' "$output"

          if [ "$status" -eq 0 ]; then
            exit 0
          fi

          case "$output" in
            *"not found"*)
              echo "::notice::Neon branch $NEON_BRANCH_NAME was already absent; cleanup is complete."
              exit 0
              ;;
          esac

          exit "$status"
