# Production Environment Variables for Render/Railway
# Copy this to your Render dashboard Environment Variables section

# Admin IP Allowlist - Add your public IPs here
# Get your current IP: curl -s https://api.ipify.org
ADMIN_IP_1=YOUR_HOME_IP_HERE
ADMIN_IP_2=YOUR_OFFICE_IP_HERE
ADMIN_IP_3=YOUR_VPN_IP_HERE

# Render automatically provides:
# - PORT (don't set this)
# - NODE_ENV=production (don't change this)

# Optional: Override log path for production
LANDING_LOG_PATH=/tmp/landing-requests.log

# Note: Localhost IPs (127.0.0.1, ::1, ::ffff:127.0.0.1) are 
# automatically included in the allowlist for local testing
