# Environment variables for local development
# Copy to .env and fill in your values

# Server Configuration
PORT=3333
NODE_ENV=development

# Supabase Configuration (optional - will use in-memory if not set)
SUPABASE_URL=
SUPABASE_KEY=

# Discord OAuth (optional)
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
API_BASE_URL=http://localhost:3333

# For production deployment:
# 1. Create Supabase project at supabase.com
# 2. Get URL and anon key from Project Settings → API
# 3. Create Discord app at discord.com/developers/applications
# 4. Set these values in your hosting platform (Render/Fly.io/etc)
