---
name: dev-web
description: Start the Next.js web app (port 3001)
---

# Dev web

From repo root:

```bash
pnpm -C apps/web dev
```

Requires root `.env` (copy from `.env.example`). Uses port **3001**.
