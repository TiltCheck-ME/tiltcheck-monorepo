# TiltCheck Dashboard - Environment Variables
# Copy to .env.local and fill in values before running

# URL of the backend API (used client-side, so must be publicly accessible)
NEXT_PUBLIC_API_URL=https://tiltcheck.me/api

# URL of the backend service (if separate from API)
NEXT_PUBLIC_BACKEND_URL=https://tiltcheck.me
